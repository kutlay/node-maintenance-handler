{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "node-maintenance-controller.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "node-maintenance-controller.resourceName" (dict "suffix" "nodemaintenance-admin-role" "context" $) }}
rules:
- apiGroups:
  - maintenance.linode.com
  resources:
  - nodemaintenances
  verbs:
  - '*'
- apiGroups:
  - maintenance.linode.com
  resources:
  - nodemaintenances/status
  verbs:
  - get
{{- end }}
